# This property file created for in-memory database

db.url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
db.username=sa
db.password=sa

db.driver=org.h2.Driver
db.dialect=org.hibernate.dialect.H2Dialect
db.showsql=true
db.schema=create